OPEN_WEBUI_PORT=3000
COMPUTER_PORT=8000

# Generate a long random value before running a real instance.
WEBUI_SECRET_KEY=replace-with-a-long-random-secret

# Optional: point Open WebUI at a local or remote Ollama server.
# OLLAMA_BASE_URL=http://host.docker.internal:11434

# Optional: use OpenAI-compatible hosted APIs in Open WebUI.
# OPENAI_API_KEY=sk-...
