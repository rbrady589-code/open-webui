# Shared Workspace

Computer sees this folder as `/workspace`.

Use it as the durable home for projects, notes, memory exports, inbox files, and generated outputs. Keep secrets out of this folder unless you intentionally manage them with a private vault or encrypted file.
